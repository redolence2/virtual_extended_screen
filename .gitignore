# === Build artifacts (kept inside project) ===
.build/
mac-host/.build/
ubuntu-client/target/

# === Local tools (downloaded, not committed) ===
tools/bin/
tools/include/
tools/lib/

# === Python ===
__pycache__/

# === OS / Editor ===
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/

# === Swift ===
# Package.resolved is committed (dependency pinning — plan v11 §11.6)
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/

# === Rust ===
# Cargo.lock is committed (dependency pinning — plan v11 §11.6)

# === Generated protobuf code (committed separately) ===
# We commit generated code, so don't ignore it

# === Secrets / local config ===
*.pem
*.key
.env
.env.local
config.local.json

# === Debug output ===
*.h264
*.hevc
*.png
*.raw
