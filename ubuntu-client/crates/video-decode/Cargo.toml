[package]
name = "video-decode"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4"
anyhow = "1"
# Exact-pinned until this legacy crate is deleted at T1 (review of the A0.0
# response, F8: prose quarantine is not operational quarantine).
ffmpeg-next = "=7.1.0"
ffmpeg-sys-next = "=7.1.3"
# EAGAIN classification for `flush()`'s send_eof retry loop (C3 / review
# finding 1 correction) — same libc::EAGAIN check `backend-construct` uses.
libc = "0.2"
