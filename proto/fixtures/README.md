# Stage-1 profile-independent binary wire fixtures

Generated by `tools/gen_fixtures.py` — do not hand-edit; regenerate with
`python3 tools/gen_fixtures.py` instead. Byte layouts and classification
rules are normative in `docs/WIRE.md`; this manifest is descriptive.

All fixtures share `session_run_id = 0x1122334455667788` and, where a
profile hash is needed, the `docs/WIRE.md` §9 placeholder `profile_hash`
`0cc2249662880597`. They are profile-independent: none of them depend on a
Stage-2-measured profile value (plan v11 §12 Stage-1 freeze scope).

Classification values (per `docs/WIRE.md`): `valid`, `MALFORMED_FRAMING`,
`PROTOCOL_VIOLATION`, `RECORD_CAP_VIOLATION`.

## Valid fixtures

| Filename | Size (B) | Expected classification | Consumed by | Notes |
|---|---|---|---|---|
| `videohello.bin` | 32 | `valid` | Swift and Rust wire tests, Stage-1 | VideoHello; session_run_id=0x1122334455667788, profile_hash=0cc2249662880597 |
| `videohelloack_ok.bin` | 16 | `valid` | Swift and Rust wire tests, Stage-1 | VideoHelloAck; status=0 OK |
| `videohelloack_mismatch.bin` | 16 | `valid` | Swift and Rust wire tests, Stage-1 | VideoHelloAck; status=1 MISMATCH |
| `videohelloack_busy.bin` | 16 | `valid` | Swift and Rust wire tests, Stage-1 | VideoHelloAck; status=2 BUSY |
| `videohelloack_internal.bin` | 16 | `valid` | Swift and Rust wire tests, Stage-1 | VideoHelloAck; status=3 INTERNAL |
| `frame_header_min.bin` | 32 | `valid` | Swift and Rust wire tests, Stage-1 | frame header; minimum legal (flags=0x01, frameOrdinal=1, payloadLen=0, header only) |
| `move.bin` | 26 | `valid` | Swift and Rust wire tests, Stage-1 | move datagram; seq=1, x=100, y=200 |
| `cursor.bin` | 43 | `valid` | Swift and Rust wire tests, Stage-1 | cursor datagram; seq=1, x=100, y=200, shape=0, hotspot 0/0, scale=1.0 |

## Malformed fixtures

| Filename | Size (B) | Expected classification | Consumed by | Notes |
|---|---|---|---|---|
| `malformed/bad_magic_hello.bin` | 32 | `PROTOCOL_VIOLATION` | Swift and Rust wire tests, Stage-1 | VideoHello; first magic byte bitwise-inverted (0x52 XOR 0xFF) |
| `malformed/bad_length_hello.bin` | 32 | `PROTOCOL_VIOLATION` | Swift and Rust wire tests, Stage-1 | VideoHello; len field = 31 (physical record is still 32 B) |
| `malformed/nonzero_reserved_hello.bin` | 32 | `PROTOCOL_VIOLATION` | Swift and Rust wire tests, Stage-1 | VideoHello; reserved u8 at offset 5 = 1 |
| `malformed/unknown_status_ack.bin` | 16 | `PROTOCOL_VIOLATION` | Swift and Rust wire tests, Stage-1 | VideoHelloAck; status = 9 (outside the frozen 0-3 range) |
| `malformed/unknown_flag_frame.bin` | 32 | `PROTOCOL_VIOLATION` | Swift and Rust wire tests, Stage-1 | frame header; flags = 0x03 (bit1 has no assigned meaning) |
| `malformed/overflow_frame.bin` | 32 | `RECORD_CAP_VIOLATION` | Swift and Rust wire tests, Stage-1 | frame header; payloadLen = 0xFFFFFFFF |
| `malformed/short_move.bin` | 25 | `PROTOCOL_VIOLATION` | Swift and Rust wire tests, Stage-1 | move datagram truncated to 25 B (of 26 B) |
| `malformed/long_cursor.bin` | 44 | `PROTOCOL_VIOLATION` | Swift and Rust wire tests, Stage-1 | cursor datagram padded to 44 B (of 43 B) with one trailing zero byte |

## Profile fixtures (JSON — generator `tools/gen_profile_fixtures.py`, except the canonical one)

`profile.canonical.json` predates the generator and is pinned by SHA-256 (`docs/WIRE.md` §9);
the generator verifies that pin as a sanity check but never rewrites the file. No profile fixture
has a trailing newline (errata implementation proof 3).

| Filename | Size (B) | Expected verdict | Consumed by | Notes |
|---|---|---|---|---|
| `profile.canonical.json` | 497 | canonical; ASCII-clean; runtime-rejected only for its `TBD-A00` placeholder backend (ERR-02) | Swift and Rust profile tests, Stage-1 | placeholder profile; SHA-256 prefix `0cc2249662880597` |
| `profile_nonascii_raw.json` | 503 | ASCII-rule rejection (ERR-07), violation at `profile_id` | Swift and Rust profile tests, Stage-1 | sorted/minified, real `sw1-lowdelay` backend, `profile_id` carries U+00E9 as raw UTF-8 — without ERR-07 nothing else is guaranteed to reject it |
| `profile_nonascii_escaped.json` | 507 | ASCII-rule rejection (ERR-07), violation at `profile_id` | Swift and Rust profile tests, Stage-1 | same document with the value written as the `\u00e9` escape — proves detection on the parsed document, not the byte stream |

## Dispatch fixtures

Generated by `tools/gen_dispatch_fixtures.py` (R5) — do not hand-edit; regenerate with
`python3 tools/gen_dispatch_fixtures.py` instead. Exercises the two-layer protocol-v3
inbound dispatch (framing-length gate + typed validator/router) AND the sender-side
note_outbound/note_video_ack routing, implemented independently in
`ubuntu-client/crates/protocol/src/v3dispatch.rs` and
`mac-host/Sources/RescCore/V3Dispatch.swift`; both are graded against these cases.

| Filename | Size (B) | Contents |
|---|---|---|
| `dispatch_cases.json` | 113622 | 335 rows: 6 framing, 164 state, 3 raw, 156 outbound, 6 video_ack (see the file's own doc comment header in the generator for the exact schema) |
| `dispatch/unknown_only_oneof.bin` | 15 | Envelope with a present run id/version and one unknown field (number 99) only — decodes to an ABSENT oneof payload |
| `dispatch/empty_envelope.bin` | 0 | zero-byte Envelope — every field decodes to its proto3 zero value |
