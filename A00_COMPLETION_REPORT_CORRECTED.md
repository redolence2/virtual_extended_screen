# A0.0 Corrected Completion Report (C→E→R attestation)

| | |
|---|---|
| **Date** | 2026-08-05 |
| **Code checkpoint C** | `8e7170f04144ecabed35eb42bba111c4c375b7c0` — the commit every matrix gate ran from |
| **Evidence commit E** | `5755c7aab7ffe3f9186a16a559c0b0d9d99e6f35` — `evidence/a00/<C>/` with 129 SHA-256-hashed artifacts + a **validating** manifest (generated by `tools/gen_evidence_manifest.py`, which fails on missing paths, hash mismatches, malformed JSON/JSONL, source dirt, any source/contract diff from C, or failed load-bearing predicates; it records `code_commit = C` and never its own containing commit's hash) |
| **Attestation (this commit, R)** | No source or contract file differs between C and R — the only commits after C are E (evidence + manifest + the reconciled r4-summary) and R (this report). The manifest generator enforces exactly this exclusion set and passed. |
| **Status claim** | The corrective cycle C1–C8 (per the response review's revised sequence) is complete and every applicable-A0.0 gate passes from C. **State 5 remains the reviewer's to grant**: A0.0 completion, Stage-1 freeze, and A0 entry stay formally pending until this report passes independent re-review; T1 additionally waits for A0, Stage 2, and its own entry checks. |

## 1. The seven blocking findings — closure by evidence

| Finding | Closed by | Sealed evidence |
|---|---|---|
| 1 — R4 receive-identity mismatch + upload-identity bug | Decode-side receipt ledger (cap 1024, fail-closed rules, queue drops excluded), `decode_trigger_frame_id` separated from recovered identity, upload-confirmed presents | Final gate: 69 joined, **0 identity ambiguities, 0 identity-failure records**, both footers clean (`final-gate.log`, `r4-live-*`) |
| 2 — R5 semantic omissions | `DispatchFacts` on inbound AND outbound; `Accepted/RemoteFatal(class)` result shape; nonzero runs, announce-only learning, bootstrap-fatal rejection, deterministic-only reject codes, oldest-outstanding ACK check, diagnostics-gated clocks; **391 vectors**, both languages verdict-identical; documented oracle review | `c1-oracle-review.md`; M3 = 624 checks, M5 = 9 suites |
| 3 — ERR-01 write-level proof | Deterministic scheduler + writer-attempt spy, 4 named cross-TCP schedules, exact retained attempt traces, both languages | `c2-summary.md`; suites in M3/M5 |
| 4.1 — SCK conversion + causal anomaly | V11 §10's literal `CMSyncConvertTime` via `stream.synchronizationClock` (output-validity guarded); the anomaly **explained and bounded**: vblank-aligned PTS leads encode-out by < one frame period (measured max 9,190 µs through the contracted path); gate slack fixed at exactly 16,667 µs, beyond which runs still fail | `c3-summary.md`; the causal-bound block in `manifest.json`; gate finding 4 commit |
| 4.2 — Global-best clock | Session-global minimum-delay sample for every joined frame | joiner + sealed `r4-live-join-summary.json` |
| 4.3 — Gate predicate | 9-condition PASS (parse-clean, clean footers both sides, zero ambiguities/failures/causal, ≥1 clock sample, ≥1 presented join, offsets everywhere, joined > 0); clean/aborted `trace_complete` footer protocol both ends; graceful-termination-only runner | 10-scenario selftest (M6); final gate PASS |
| 5 — Zero-output substitution | **Real zero-output forced on selected sw1-lowdelay** (param-set-only packet: accepted, empty drain; 480/480 exactly-once; byte-identical reruns) — **ERR-09 never needed**; cuvid supplementary | `c4-summary.md`, `c4-*-zero-output.json`, U4 |
| 6 — Fail-open runner | Fail-closed v2: run tokens, per-run `doctor_complete` deltas == 1, real `doctor_host.json` parsed, all exits captured incl. remote receivers, fatal copy failures, `sender_integrity_pass` + full v2 predicates | `c5-summary.md`; X1 ALL GREEN (`final-r3b.log`) |
| 7 — Evidence seal | C→E→R topology; validating generator; per-gate command/machine/exit; environments; 129 hashed artifacts | `manifest.json` (E) |

## 2. The final matrix (all from C; 13 gates, all PASS)

Mac: regen-check · generators-leave-tree-clean · 624 fixture checks / 0 FAIL · host doctor ·
9 cargo suites · joiner selftest. Ubuntu (same commit, dirty 0): locked release build · 22
locked suites · client doctor (sw1, full sample, texture-update) · 6 decoder runs
(characterize/clean/force-zero-output × both backends). Cross: fail-closed r3b ALL GREEN
(token `r3b-20260805042947-82162`) · live joined-trace gate PASS (69 joined / 57
presented-joined / 0 ambiguities / 0 failures / 3 counted connect-window drops).

## 3. What the hardened gate itself caught during C7 (the cycle's strongest validation)

Four consecutive fail-closed refusals, each a distinct real defect, each fixed under its own
checkpoint with a full matrix rerun: (1) concurrent SIGTERM let the host die first — client
exited footerless via control-EOF (fix: client-first ordered termination); (2) bash backgrounds
a `cd && nohup` compound as a subshell, so `$!` was a wrapper pid and SIGTERM orphaned the
client (reproduced in isolation; fix: statement-separated launch); (3) suppressed empty-plane
emissions were skipped before their receipt-ledger resolve — 29 phantom pending identities,
honestly `aborted` footer (fix: identity-before-skip at all three emission sites); (4) the
causal check fired on genuine vblank-lead semantics — explained, measured, bounded at one frame
period. The pre-corrective joiner would have silently passed all four runs.

## 4. Carried decisions (unchanged)

Backend **sw1-lowdelay**, `decoder_lag_bound_frames = 1`, `output_deadline_ms = 50` (fixed for
A0; written into the canonical profile at Stage-2 freeze). Contract errata this whole effort:
ERR-06/07/08 (ERR-09 written-but-never-needed). Every repo status line reads *candidate*.

## 5. Phase position

C1–C8 complete → **this report to independent re-review** → on GO: A0.0 complete, Stage 1
freezes, A0 begins (real-motion histogram, software latency baseline + optical spot-check,
window trial, bitrate confirmation, Stage-2 profile freeze) → T1 only after its own entry
checks. Until then every downstream phase remains NO-GO.
